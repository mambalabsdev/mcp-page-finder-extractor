{
  "manifest_version": "0.3",
  "name": "mcp-page-finder-extractor",
  "version": "1.0.0",
  "description": "MCP server for the Mamba Labs Page Finder and Extractor actor on Apify. Company domain in, the named page type located on that company's own website out, with the method that found it and a confidence for that method.",
  "author": {
    "name": "Mamba Labs"
  },
  "license": "MIT",
  "homepage": "https://github.com/mambalabsdev/mcp-page-finder-extractor",
  "repository": {
    "type": "git",
    "url": "https://github.com/mambalabsdev/mcp-page-finder-extractor"
  },
  "server": {
    "type": "node",
    "entry_point": "build/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/build/index.js"
      ],
      "env": {
        "APIFY_TOKEN": "${user_config.apify_token}"
      }
    }
  },
  "user_config": {
    "apify_token": {
      "type": "string",
      "title": "Apify API Token",
      "description": "Apify API token for running the actor",
      "sensitive": true,
      "required": false
    }
  }
}
